---
# Vault Backend (consul s3)
vault_backend: consul

# Vault Group
vault_group: "vault"
vault_gid: "10012"

# Vault User
vault_user: "vault"
vault_uid: "10012"

# Vault Application
vault_version: "1.1.3"

# Vault Config
vault_consul: "127.0.0.1:8500"
vault_consul_path: "vault"
vault_address: "0.0.0.0"
vault_port: "8200"
vault_bucket_name: "vault-bucket"

# Start Vault
vault_start: True
